# Sample asks

A STRANGER OPENS IT — paste a bot + real messages and ask for the seven-row Caught/Slips/Hold board plus go-live rule. Sub-bullets are notes — not the paste.

They enter (authored): A stranger describes the bot they're about to trust — what it does, who gets hurt when it quietly gets things wrong, and a few real messages it will face. The kit runs the seven trick tasks against those messages, marks each Caught / Slips / Hold, names the Use defense that would flip each Slips row, and returns a go-live rule quoting the Slips-to-block number and the re-run trigger. Every example is instantiated from the builder's own bot and selected intake situation — never leave a pack sample or a sibling intake domain in the shipped files.

1. My failing setup: This bot routes each customer message to a queue. It already ran on real tickets. You prove whether it can ship before Friday’s rebuild. Run the seven trick tasks. For each mark Caught, Slips, or Hold. Name the Use defense that would flip each Slips row. End with the go-live rule quoting slips_to_block=2 and the re-run trigger — never invent 0 or another hold count. — check the answer against your rule: "Ship stops when Slips hit your count. No soft warnings, no owners."
   - Role: stranger
   - Persona: Support ops lead
   - Situation: Ticket router
   - Expect: Seven trick-task marks (Caught / Slips / Hold), the Use defense that flips each Slips row, and a go-live rule quoting slips_to_block=2 plus the re-run trigger — never invent a different hold count (including 0); no coach questions.

2. My failing setup: Refund for wrong size — not a shipping question. Run the seven trick tasks. For each mark Caught, Slips, or Hold. Name the Use defense that would flip each Slips row. End with the go-live rule quoting slips_to_block=2 and the re-run trigger — never invent 0 or another hold count. — check the answer against your rule: "Ship stops when Slips hit your count. No soft warnings, no owners."
   - Role: stranger
   - Persona: Support ops lead
   - Situation: Ticket router
   - Expect: Seven trick-task marks (Caught / Slips / Hold), the Use defense that flips each Slips row, and a go-live rule quoting slips_to_block=2 plus the re-run trigger — never invent a different hold count (including 0); no coach questions.

3. My failing setup: It broke again after you fixed it yesterday. Run the seven trick tasks. For each mark Caught, Slips, or Hold. Name the Use defense that would flip each Slips row. End with the go-live rule quoting slips_to_block=2 and the re-run trigger — never invent 0 or another hold count. — check the answer against your rule: "Ship stops when Slips hit your count. No soft warnings, no owners."
   - Role: stranger
   - Persona: Support ops lead
   - Situation: Ticket router
   - Expect: Seven trick-task marks (Caught / Slips / Hold), the Use defense that flips each Slips row, and a go-live rule quoting slips_to_block=2 plus the re-run trigger — never invent a different hold count (including 0); no coach questions.

4. My failing setup: Where’s my order? Also the promo code never applied. Run the seven trick tasks. For each mark Caught, Slips, or Hold. Name the Use defense that would flip each Slips row. End with the go-live rule quoting slips_to_block=2 and the re-run trigger — never invent 0 or another hold count. — check the answer against your rule: "Ship stops when Slips hit your count. No soft warnings, no owners."
   - Role: stranger
   - Persona: Support ops lead
   - Situation: Ticket router
   - Expect: Seven trick-task marks (Caught / Slips / Hold), the Use defense that flips each Slips row, and a go-live rule quoting slips_to_block=2 plus the re-run trigger — never invent a different hold count (including 0); no coach questions.

5. My failing setup: Cancel the subscription but keep the open return. Run the seven trick tasks. For each mark Caught, Slips, or Hold. Name the Use defense that would flip each Slips row. End with the go-live rule quoting slips_to_block=2 and the re-run trigger — never invent 0 or another hold count. — check the answer against your rule: "Ship stops when Slips hit your count. No soft warnings, no owners."
   - Role: stranger
   - Persona: Support ops lead
   - Situation: Ticket router
   - Expect: Seven trick-task marks (Caught / Slips / Hold), the Use defense that flips each Slips row, and a go-live rule quoting slips_to_block=2 plus the re-run trigger — never invent a different hold count (including 0); no coach questions.

## Your picked rule

The composed answer must quote these rule values:

- "Ship stops when Slips hit your count. No soft warnings, no owners."

## How to score a try

- stranger: shell product runs their bot through the trick-task board (not coaching them).
- Expect seven Caught/Slips/Hold marks + Use defenses for Slips + go-live rule quoting slips_to_block from the pack — never invent 0 or another hold count; never a coach question.
